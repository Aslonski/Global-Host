<div class="container">
	<div class="login-container">
		<h1 class="user-interaction-title">Sign up!</h1>
		<%= render '/errors' %>

		<%= simple_form_for :user, url: '/users' do|f| %>
		  <%= f.input :first_name, label: 'First Name' %>
		  <%= f.input :last_name, label: 'Last Name' %>
		  <%= f.input :email, label: 'Email', placeholder: 'user@domain.com' %>
			<%= f.input :city, label: 'City' %>
		  <%= f.input :state_province, label: 'State/Province' %>
			<%= f.input :country, label: 'Country' %>
		  <%= f.input :personal_info, label: 'Bio' %>
		  <%= f.input :language, label: 'Language' %>
		  <%= f.input :gender, label: 'Gender' %>
			<%= f.input :password, label: 'Password' %>
		 <%= f.button :submit, label: 'Submit' %>
		<% end %>
	</div>
</div>
