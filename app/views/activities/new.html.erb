<div class="row">
  <div class="container col s12">
    <%= form_for [@itinerary, @activity] do |f| %>
      <%= f.label :description %>:
      <%= f.text_field :description %>
      <%= f.submit "Create New Activity", :class => "waves-effect waves-light btn user-button" %>
    <% end %>
  </div>
</div>
